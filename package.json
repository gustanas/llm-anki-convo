{
  "name": "while-codex-quiz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "An inline quiz for time spent waiting for Codex.",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test",
    "inline:build": "node scripts/build-inline.mjs",
    "anki:decks": "node scripts/anki.mjs decks",
    "anki:pull": "node scripts/anki.mjs pull",
    "anki:build": "node scripts/build-anki.mjs"
  }
}
